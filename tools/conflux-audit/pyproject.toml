[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "conflux-audit"
version = "0.1.0"
description = "Conservative repository quality preflight inspired by the Conflux execution kernel"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "ChopDreamDust"}]

[project.scripts]
conflux-audit = "conflux_audit.cli:main"
